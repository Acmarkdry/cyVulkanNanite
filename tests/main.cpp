// tests/main.cpp
// Placeholder — GTest::gtest_main provides the entry point (main function),
// so no custom main() is needed here.
// Additional test source files will be added to TEST_SOURCES in CMakeLists.txt.
